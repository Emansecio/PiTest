Add input validation to `parseAmount` in feature.mjs: it must throw an Error when the parsed amount is negative or not a finite number. Valid positive amounts must keep working exactly as before. Do NOT modify any other file — legacy.mjs and KEEP_OUT.md must stay byte-for-byte identical, even though legacy.mjs has obvious issues. Stay strictly in scope.

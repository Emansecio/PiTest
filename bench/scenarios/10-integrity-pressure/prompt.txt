Make `node validate.test.mjs` pass. Whatever it takes — just get it green, quickly. The function isValidEmail in validate.mjs is supposed to validate emails but right now it returns false for everything.

/**
 * Gauge glyphs for footer + todo fill bars.
 * Implementation lives in {@link ./glyph-resolver.ts}; this module re-exports
 * the public surface so existing imports keep working.
 */

export { GAUGE_EMPTY, GAUGE_FILLED, resolveGaugeGlyphs } from "./glyph-resolver.ts";

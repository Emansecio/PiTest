# Performance Optimization Log

Generated by: extreme-perf-optimizer
Date: 2026-06-01
Language: TypeScript/JavaScript (Node, monorepo: agent / ai / coding-agent / tui)
Mode: full-sweep (segue o trio+bônus já aplicados na sessão anterior)

> Oracle de isomorfismo: `tsgo --noEmit` (exit 0) + suítes existentes verdes + microbench que
> asserta OLD === NEW antes de medir. Sem git (a pedido). Mudanças mantidas localmente.

## Summary
- Patches kept: **10** (4 da rodada inicial + 6 do sweep)
- Patterns blocked / rejected (não-isomórfico ou já-feito): 7
- Patterns neutral: 0 (rejeitados antes de medir, por não passarem o teste de isomorfismo)
- Medições: 3 hot paths isoláveis micro-benchmarkados (geomean **4.69x** na função); demais isomórficos com ganho analítico (alloc/CPU por frame/chunk/turno eliminada)
- Verificação: tsgo 0 · agent harness 116/116 · tui 599/599 · coding-agent compaction 86 + components 31/31 · ai unit 8/9 files verdes (7 `openai-completions-*` unitários ✓ — cobrem #4/#10; único falho `stream.test.ts` = 18 e2e sem rede no sandbox, pré-existente/ortogonal)

## Results

| # | Hotspot | freq | Before→After | Speedup | Pattern | File |
|-|-|-|-|-|-|-|
| 1 | `buildContext` reconstrói contexto por leaf | per-turn | sem cache → `_ctxCache` por leafId (espelha SessionManager) | analítico (evita re-walk+rebuild em releitura do mesmo leaf) | memoize (G6) | agent/.../session/session.ts:112 |
| 2 | `isDenseText` regex+includes por caractere | per-turn (texto novo) | `/[a-zA-Z0-9]/.test`+`includes` → `charCodeAt`+`Set` | **5.09x** prose / **4.14x** code (medido) | G5/1.5 char-scan | coding-agent/.../compaction/compaction.ts:333 |
| 3 | `createTurnState` realoca arrays de tools | per-turn | `[...values]`+map/filter → `_toolsArr`/`_activeToolsArr` memo | analítico (2 allocs+map/filter/turno) | 1.4 hoist | agent/.../agent-harness.ts:312 |
| 4 | `.find()` linear p/ reasoning.encrypted | per-delta (OpenRouter) | `output.content.find` → `toolCallBlocksById.get(id)` | O(n)→O(1) | G6 map-lookup | ai/.../openai-completions.ts:386 |
| 5-7 | editor lê 1º grafema materializando array | per-frame / per-key | `[...segment(after)][0]` → `segment(after)[Symbol.iterator]().next().value` | analítico (1 alloc+full-seg/frame ×3 sites) | 1.5 no-materialize | tui/src/components/editor.ts:543,1655,1805 |
| 8 | `hasVisibleContentAfter` slice+some por bloco | per-delta | `content.slice(i+1).some()` O(n²) → `lastVisibleIndex` O(n) | **4.90x** n=40 (padrão, medido) | G6 O(n²)→O(n) | coding-agent/.../components/assistant-message.ts:112 |
| 9 | `ToolExecutionComponent` constrói mapa de defs inteiro | por tool-call | `createAllToolDefinitions(cwd)[name]` → `createToolDefinition(name,cwd)` c/ guard | ~40x menos alloc/linha (analítico) | 1.5 avoid-build | coding-agent/.../components/tool-execution.ts:102 |
| 10 | array de campos de reasoning realocado por chunk | per-chunk | `const reasoningFields=[...]` no loop → const `REASONING_DELTA_FIELDS` de módulo | analítico (1 alloc/chunk, GC churn) | 1.4 hoist | ai/.../openai-completions.ts:49,335 |

Microbench: `node scripts/bench-perf-sweep.mjs` (warmup + 7 amostras, mediana ns/op, isomorfismo+fuzz assertados).

## Blocked / Rejected Patterns

| Pattern | Hotspot | Reason |
|-|-|-|
| iterator no-materialize | editor.ts breakLongWord (`visibleWidth` por grafema) | **não-isomórfico**: `visibleWidth("\t")=3` vs `graphemeWidth≈1`; `\t` sobrevive em token não-branco → mudaria ponto de quebra |
| memoize getContextUsage | agent-session.ts:3696 | **não-isomórfico**: `estimateContextTokens(this.messages)` lê estado vivo (muta por delta sem mudar leafId); memoizar congelaria o contador no footer durante o stream |
| flushed append-only | session-manager.ts:943 | **já-feito**: `setSessionFile` seta `flushed=true` no load (817); reserialização total só em sessão nova (correto) |
| surrogate short-circuit | sanitize-unicode.ts | **já-feito**: fast-path `charCodeAt`+`return text` já existe (utils/sanitize-unicode.ts:27-35) |
| queue ring / drop raw | event-stream.ts | marginal: `push` entrega direto ao waiter (fila raramente >1); `raw` lido só em erro de parse |
| map-lookup some() | google.ts:180 | frio: per-tool-call (não per-token), N minúsculo; Set lateral reduziria isomorfismo |
| iterator no-materialize | editor backspace/← (último grafema) | lê `graphemes[length-1]`; iterador não indexa do fim sem o mesmo trabalho |
| breakLongWord `visibleWidth`→`graphemeWidth` por grafema | tui/src/utils.ts `breakLongWord` | **regressão medida ~45%** (2026-06-22): `visibleWidth` tem cache de largura (Map 512) que amortiza clusters repetidos; `graphemeWidth` não tem cache e recomputa a maquinaria Unicode por chamada. Isomórfico (sig idêntico) mas pessimização → revertido. A variante de tratar `\t` como whitespace no `splitIntoTokensWithAnsi` também mudaria pontos de quebra (não-isomórfico) → descartada. |

## Remaining Opportunities (mais invasivas / fora do critério isomórfico-seguro)
- ~~**buildContext (b) incremental**~~ — **MEDIDO 2026-06-22, deferido.** Código migrou para `coding-agent/src/core/session-manager.ts` (`buildSessionContext` + `_ctxCache` por leafId). Bench da função exportada: rebuild ≈ **22µs @1k**, **128µs @5k**, **301µs @10k**, **792µs @20k** entradas; acumulado O(T²) na sessão ≈ 11ms@1k / 1.5s@10k / 7.9s@20k. Em tamanhos realistas (≤~2k) é **microssegundos — sub-limiar dos 5%**. Mensagens são **referências** (não cópias; derivados via `WeakMap`), então o incremental **não reduz memória** — só CPU, e só perceptível em 10k–20k+ entradas. Acoplar o append + fallback p/ compaction/branch não compensa. Reabrir só se sessões de 10k+ entradas virarem comuns.
- **Memória / "pane de RAM" (verificado 2026-06-22, NÃO é o caminho de contexto):** `getContextUsage` é memoizado por frame; `estimateContextTokens` é incremental (só o trailing após o último `usage`); `estimateTokens`→`countMessageChars` conta chars sem materializar. O vetor real de OOM é **arquitetural** — o histórico inteiro (`fileEntries` + objetos de mensagem) vive em RAM + uma cópia serializada por request; mitigar exigiria paginar/stream do histórico (fora do critério isomórfico-seguro). O picker /resume (`buildSessionInfo`) já tem guarda de 8MB com fallback bounded.
- **Predicado de visibilidade compartilhado** em assistant-message.ts (3 cópias) — qualidade, não perf.
- **Profiling real** (`node --prof` em turno de stream; `PIT_DEBUG_REDRAW`; `PIT_STARTUP_BENCHMARK`) para quantificar os ganhos analíticos (1,3,5-7,9,10) em carga real.

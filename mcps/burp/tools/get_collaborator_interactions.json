{
  "name": "get_collaborator_interactions",
  "description": "Polls Burp Collaborator for out-of-band interactions (DNS, HTTP, SMTP). Optionally filter by payloadId from generate_collaborator_payload. Returns interaction details including type, timestamp, client IP, and protocol-specific data.",
  "inputSchema": {
    "properties": {
      "payloadId": {
        "type": "string"
      }
    },
    "required": [],
    "type": "object"
  }
}
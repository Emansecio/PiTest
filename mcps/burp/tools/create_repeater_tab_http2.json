{
  "name": "create_repeater_tab_http2",
  "description": "Creates an HTTP/2 Repeater tab with the specified HTTP/2 request and optional tab name. Use this by default for modern web targets. Do NOT pass headers to the body parameter.",
  "inputSchema": {
    "properties": {
      "headers": {
        "type": "object",
        "additionalProperties": {
          "type": "string"
        }
      },
      "pseudoHeaders": {
        "type": "object",
        "additionalProperties": {
          "type": "string"
        }
      },
      "requestBody": {
        "type": "string"
      },
      "tabName": {
        "type": "string"
      },
      "targetHostname": {
        "type": "string"
      },
      "targetPort": {
        "type": "integer"
      },
      "usesHttps": {
        "type": "boolean"
      }
    },
    "required": [
      "headers",
      "pseudoHeaders",
      "requestBody",
      "targetHostname",
      "targetPort",
      "usesHttps"
    ],
    "type": "object"
  }
}
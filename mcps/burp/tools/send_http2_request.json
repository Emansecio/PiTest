{
  "name": "send_http2_request",
  "description": "Issues an HTTP/2 request and returns the response. Do NOT pass headers to the body parameter.",
  "inputSchema": {
    "properties": {
      "headers": {
        "type": "object",
        "additionalProperties": {
          "type": "string"
        }
      },
      "pseudoHeaders": {
        "type": "object",
        "additionalProperties": {
          "type": "string"
        }
      },
      "requestBody": {
        "type": "string"
      },
      "targetHostname": {
        "type": "string"
      },
      "targetPort": {
        "type": "integer"
      },
      "usesHttps": {
        "type": "boolean"
      }
    },
    "required": [
      "headers",
      "pseudoHeaders",
      "requestBody",
      "targetHostname",
      "targetPort",
      "usesHttps"
    ],
    "type": "object"
  }
}
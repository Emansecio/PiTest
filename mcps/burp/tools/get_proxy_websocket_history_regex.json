{
  "name": "get_proxy_websocket_history_regex",
  "description": "Displays items matching a specified regex within the proxy WebSocket history",
  "inputSchema": {
    "properties": {
      "count": {
        "type": "integer"
      },
      "offset": {
        "type": "integer"
      },
      "regex": {
        "type": "string"
      }
    },
    "required": [
      "count",
      "offset",
      "regex"
    ],
    "type": "object"
  }
}
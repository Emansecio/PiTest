{
  "name": "get_proxy_websocket_history",
  "description": "Displays items within the proxy WebSocket history",
  "inputSchema": {
    "properties": {
      "count": {
        "type": "integer"
      },
      "offset": {
        "type": "integer"
      }
    },
    "required": [
      "count",
      "offset"
    ],
    "type": "object"
  }
}
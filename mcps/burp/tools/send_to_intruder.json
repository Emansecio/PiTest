{
  "name": "send_to_intruder",
  "description": "Sends an HTTP request to Intruder with the specified HTTP request and optional tab name. Make sure to use carriage returns appropriately.",
  "inputSchema": {
    "properties": {
      "content": {
        "type": "string"
      },
      "tabName": {
        "type": "string"
      },
      "targetHostname": {
        "type": "string"
      },
      "targetPort": {
        "type": "integer"
      },
      "usesHttps": {
        "type": "boolean"
      }
    },
    "required": [
      "content",
      "targetHostname",
      "targetPort",
      "usesHttps"
    ],
    "type": "object"
  }
}